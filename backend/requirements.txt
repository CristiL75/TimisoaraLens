# FastAPI Backend
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.6
pydantic==2.10.3
pydantic[email]
email-validator==2.3.0
python-dotenv==1.0.0

# Google OAuth
google-auth==2.25.2
# httpx: upgraded to 0.28.1 after removing ollama dependency below
httpx==0.28.1

# Web Scraping & Data Processing
requests==2.31.0
beautifulsoup4==4.12.2

# RAG - Proxy only (heavy deps moved to HF Space)
# qdrant-client, sentence-transformers, torch, transformers -> now in hf-space-rag/

# Computer Vision - TO BE ADDED LATER
# torch==2.2.0
# torchvision==0.17.0
# opencv-python==4.9.0.80
# Pillow==10.2.0

# Image Processing - TO BE ADDED LATER
# numpy==1.26.3
# scikit-learn==1.4.0

# Optional: Speech (pentru viitor)
# openai-whisper==20231117
# TTS==0.22.0

# Database - MongoDB
motor==3.3.2
pymongo==4.6.1
python-dateutil==2.8.2

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# HTTP & CORS
# httpx is already pinned above to 0.28.1. Removed the duplicate/conflicting constraint to avoid resolution errors during deployment.

# Utilities (COMMENTED OUT heavy ML/data packages - uncomment when needed)
# pandas: pin to >=2.2.3 to fix Python 3.13 C++ attribute compilation issues on Render
# pandas>=2.2.3
requests==2.31.0
# tqdm==4.66.1
